enum RecoveryPermissions {
    Dump = "recovery:dump",
    Restore = "recovery:restore",
    FileBackup = "recovery:fileBackup",
    FileRestore = "recovery:fileRestore"
}

export { RecoveryPermissions };
export default RecoveryPermissions;
