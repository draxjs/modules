import {IdentityPermissions} from "@drax/identity-back"

const role = {
  name: "Operator",
  icon: 'mdi-face-agent',
  color: 'orange',
  permissions: [

  ],
  childRoles: [
  ],
  readonly: true
}

export default role
