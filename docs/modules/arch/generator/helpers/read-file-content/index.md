# readFileContent

## Propósito

`readFileContent` lee un archivo UTF-8 y devuelve su contenido como string.

## Cuándo usarlo

Es un helper de soporte para templates o procesos de generación que necesiten tomar contenido desde disco. En el estado actual del módulo sirve como utilidad disponible dentro del paquete.
