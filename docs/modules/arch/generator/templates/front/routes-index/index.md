# RoutesIndex

## Propósito

`TemplateRoutesIndex` genera el `index.ts` de rutas para un módulo frontend.

## Qué hace

Importa todas las rutas CRUD de las entidades del módulo y las exporta como un arreglo único, listo para montar en el router principal.
