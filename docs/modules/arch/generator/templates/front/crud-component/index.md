# CrudComponent

## Propósito

`TemplateCrudComponent` genera el componente visual principal del CRUD.

## Qué agrega

Renderiza `<crud>` de `@drax/crud-vue` y define slots para presentar mejor referencias, arrays, fechas y archivos dentro de la grilla.
