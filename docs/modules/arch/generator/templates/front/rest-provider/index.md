# RestProvider

## Propósito

`TemplateRestProvider` genera un provider singleton basado en `AbstractCrudRestProvider`.

## Qué resuelve

Expone la ruta REST de la entidad y respeta `apiBasePath` cuando está definido en el schema, como ocurre en el ejemplo `CountrySchema`.
