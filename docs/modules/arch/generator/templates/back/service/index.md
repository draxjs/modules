# Service

## Propósito

`TemplateService` genera un servicio basado en `AbstractService`.

## Qué hace

Recibe un repositorio tipado y, opcionalmente, los schemas Zod base y completos. Además activa validación de salida para que la entidad serializada respete el contrato.
