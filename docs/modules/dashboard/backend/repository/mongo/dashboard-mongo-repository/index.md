# DashboardMongoRepository

## Propósito

`DashboardMongoRepository` implementa `IDashboardRepository` sobre `AbstractMongoRepository`.

## Configuración

- usa `DashboardModel` como `_model`
- permite búsqueda por `identifier` y `title`
- no usa `populateFields`
