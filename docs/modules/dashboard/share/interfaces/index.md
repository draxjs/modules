# Interfaces

## Propósito

La carpeta agrupa los contratos compartidos del módulo.

## Piezas principales

- `IDashboardCard`: tarjeta individual del dashboard.
- `IDashboardBase`: payload base.
- `IDashboard`: versión persistida con `_id`.
