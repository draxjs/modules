# DashboardProvider

## Propósito

`DashboardProvider` encapsula el transporte REST del módulo sobre `AbstractCrudRestProvider`.

## Configuración

- base URL `/api/dashboard`
- singleton accesible en `DashboardProvider.instance`
- tipado con `IDashboard` e `IDashboardBase`
