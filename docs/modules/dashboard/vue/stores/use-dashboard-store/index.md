# useDashboardStore

## Propósito

`useDashboardStore` guarda dashboards disponibles en una store Pinia.

## Qué expone

- estado `dashboards`
- getter `getDashboards`
- acción `setDashboards()`
- acción `addEntity()` para agregar un dashboard al arreglo actual
