# MediaRoutes

## Propósito

`MediaRoutes` publica los endpoints binarios del módulo.

## Endpoints

- `POST /api/file/:dir`
- `GET /api/file/:dir/:year/:month/:filename`

## Rol

El primero sube un archivo al directorio lógico indicado y el segundo lo descarga desde storage.
