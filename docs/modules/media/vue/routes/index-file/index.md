# index

## Propósito

El `index.ts` de `routes` consolida la lista de rutas exportadas del módulo.

## Cómo funciona

- importa `FileCrudRoute`
- exporta `routes = [...FileCrudRoute]`
- también lo exporta como default
