# FileCrud

## Propósito

`FileCrud.vue` monta el CRUD visual de archivos.

## Personalizaciones

- usa `Crud` con `FileEntityCrud.instance`
- formatea `tags`, `size` y fechas
- cuando la operación es `view`, usa `MediaFieldView` como formulario de detalle
