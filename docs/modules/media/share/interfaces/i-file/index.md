# IFile

## Propósito

`IFile` e `IFileBase` describen la metadata compartida del archivo.

## Campos

Incluyen nombre, paths, URL, MIME, tamaño, expiración, autoría y contador de hits.

## Diferencia

- `IFileBase` es el payload base
- `IFile` agrega `_id`
