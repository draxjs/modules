# MediaSystem

## Propósito

`MediaSystem` envuelve un `IMediaProvider` y expone la operación de upload.

## Método principal

- `uploadFile(file, dir, timeout)`

## Rol

Sirve como fachada estable para componentes y formularios que suben archivos.
