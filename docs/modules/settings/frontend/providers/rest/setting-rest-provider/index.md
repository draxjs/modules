# SettingRestProvider

## Propósito

`SettingRestProvider` implementa `ISettingProvider` sobre `IHttpClient`.

## Endpoints usados

- `GET /api/settings`
- `GET /api/settings/grouped`
- `GET /api/settings/:key`
- `PATCH /api/settings/:id`
