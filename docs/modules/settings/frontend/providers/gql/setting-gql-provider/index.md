# SettingGqlProvider

## Propósito

`SettingGqlProvider` implementa `ISettingProvider` sobre un cliente GraphQL.

## Estado actual

Los métodos `fetchAll`, `fetchGrouped`, `findByKey` y `updateValue` todavía lanzan `Not implemented`, por lo que hoy funciona como stub.
