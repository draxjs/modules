# Mongo

## Propósito

La subcarpeta `mongo` implementa persistencia de identidad sobre MongoDB/Mongoose.

## Clases

- `RoleMongoRepository`
- `TenantMongoRepository`
- `UserApiKeyMongoRepository`
- `UserLoginFailMongoRepository`
- `UserMongoRepository`
- `UserSessionMongoRepository`
