# TenantSchema

## Propósito

`TenantSchema` define los contratos Zod de tenants.

## Schemas

- `TenantBaseSchema`
  - `name`
- `TenantSchema`
  - agrega `_id`, `id?`, `custom?`, `createdAt?`, `updatedAt?`
