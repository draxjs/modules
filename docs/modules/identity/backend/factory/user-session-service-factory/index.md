# UserSessionServiceFactory

## Propósito

`UserSessionServiceFactory` crea o reutiliza una instancia singleton de `UserSessionService`.

## Persistencia

- `UserSessionMongoRepository`
- `UserSessionSqliteRepository` con `build()`
