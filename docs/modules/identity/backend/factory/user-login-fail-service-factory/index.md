# UserLoginFailServiceFactory

## Propósito

`UserLoginFailServiceFactory` crea o reutiliza una instancia singleton de `UserLoginFailService`.

## Persistencia

- `UserLoginFailMongoRepository`
- `UserLoginFailSqliteRepository` con `build()`
