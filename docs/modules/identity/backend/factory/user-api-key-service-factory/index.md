# UserApiKeyServiceFactory

## Propósito

`UserApiKeyServiceFactory` crea o reutiliza una instancia singleton de `UserApiKeyService`.

## Persistencia

- `UserApiKeyMongoRepository`
- `UserApiKeySqliteRepository` con `build()`
