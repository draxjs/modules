# UserLoginFailService

## Propósito

`UserLoginFailService` es una especialización mínima de `AbstractService` para intentos fallidos de login.

## Particularidad

No agrega lógica de negocio adicional; se limita a tipar el servicio con `IUserLoginFail`.
