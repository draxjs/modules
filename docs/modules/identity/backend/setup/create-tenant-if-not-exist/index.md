# CreateTenantIfNotExist

## Propósito

`CreateTenantIfNotExist` crea un tenant solo si no existe otro con el mismo nombre.

## Flujo

- busca por nombre
- si no existe, lo crea
- si ya existe, no hace nada
