# RoleCombobox

## Propósito

`RoleCombobox.vue` permite seleccionar roles desde la UI.

## Qué hace

Carga roles con `useRole()`, soporta modo múltiple, personaliza `itemTitle` y `itemValue`, y traduce la etiqueta con `vue-i18n`.
