# PermissionCombobox

## Propósito

`PermissionCombobox.vue` ofrece un selector múltiple de permisos.

## Qué hace

Carga los permisos desde `useRole().fetchPermissions()` al montar y los presenta en un `v-select` múltiple.
