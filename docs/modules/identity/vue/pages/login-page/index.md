# LoginPage

## Propósito

`LoginPage.vue` compone la página completa de login.

## Qué hace

Muestra branding configurable por `VITE_TITLE_MAIN` y `VITE_TITLE_SEC`, monta `IdentityLogin` y redirige a `/` tras un login exitoso.
