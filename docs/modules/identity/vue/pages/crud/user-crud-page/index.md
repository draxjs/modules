# UserCrudPage

## Propósito

`UserCrudPage.vue` compone la pantalla CRUD de usuarios.

## Qué hace

Integra `UserForm`, `UserPasswordDialog`, `PasswordUpdateButton` e `IdentityUserGroupBy`, y customiza avatar, rol, acciones y estado activo.
