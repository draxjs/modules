# RoleCrudPage

## Propósito

`RoleCrudPage.vue` compone la pantalla CRUD de roles.

## Qué hace

Usa `useCrud(identityCrudStore.roleCrud)`, integra `RoleForm` y customiza renders de permisos, child roles y readonly.
