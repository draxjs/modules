# IdentityRoutes

## Propósito

`IdentityRoutes.ts` compone el arreglo total de rutas de `identity-vue`.

## Qué hace

Concatena rutas de auth, CRUD, `UserLoginFailCrudRoute` y `UserSessionCrudRoute`.
