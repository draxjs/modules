# UserLoginFailCrudRoute

## Propósito

`UserLoginFailCrudRoute.ts` define la ruta aislada para intentos fallidos.

## Qué hace

Registra `/identity/user-login-fail` con permiso `userloginfail:manage`.
