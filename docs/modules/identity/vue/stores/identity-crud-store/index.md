# IdentityCrudStore

## Propósito

`IdentityCrudStore.ts` centraliza las instancias CRUD del módulo identidad.

## Qué hace

Mantiene en estado `userCrud`, `userApiKeyCrud`, `roleCrud` y `tenantCrud`, además de setters para reemplazarlas.
