# useAuth

## Propósito

`useAuth` concentra la integración Vue con auth.

## Qué hace

Coordina `AuthStore`, `AuthSystemFactory` y `vue-router` para login, logout, token login, permisos, recuperación, registro, avatar y fetch del usuario autenticado.
