# RoleForm

## Propósito

`RoleForm.vue` implementa el formulario CRUD de roles.

## Qué hace

Integra `IconCombobox`, `ColorCombobox`, `RoleCombobox` y `PermissionSelector`, y adapta el variant según la operación del CRUD store.
