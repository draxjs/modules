# RoleCrud

## Propósito

`RoleCrud` define la entidad CRUD de roles.

## Qué hace

Configura permisos, headers, fields como `icon`, `color`, `childRoles` y `permissions`, y usa `RoleSystemFactory` como provider.
