# UserPasswordDialog

## Propósito

`UserPasswordDialog.vue` abre el flujo modal para cambiar la contraseña de un usuario.

## Qué hace

Usa `UserPasswordForm`, delega el cambio a `UserSystemFactory` y muestra loading, errores y estado de éxito.
