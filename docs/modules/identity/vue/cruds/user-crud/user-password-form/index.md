# UserPasswordForm

## Propósito

`UserPasswordForm.vue` encapsula el formulario interno de nueva contraseña.

## Qué hace

Modela `IUserPassword`, valida confirmación, muestra `inputErrors` y emite `formSubmit`.
