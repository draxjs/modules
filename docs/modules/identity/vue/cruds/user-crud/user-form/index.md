# UserForm

## Propósito

`UserForm.vue` implementa el formulario CRUD de usuarios.

## Qué hace

Integra `RoleCombobox`, `TenantCombobox`, validaciones por operación y un field de password solo para creación.
