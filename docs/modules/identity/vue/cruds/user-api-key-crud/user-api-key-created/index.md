# UserApiKeyCreated

## Propósito

`UserApiKeyCreated.vue` muestra el secreto recién generado de una API key.

## Qué hace

Abre un diálogo persistente, muestra `userApiKey.secret` y ofrece copiarlo al portapapeles con `useCopy()`.
