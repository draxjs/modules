# UserApiKeyCrud

## Propósito

`UserApiKeyCrud` define la entidad CRUD de API keys.

## Qué hace

Configura permisos, headers de red y fechas, fields `name`, `ipv4` e `ipv6`, y usa `UserApiKeySystemFactory` como provider.
