# IdentityLogin

## Propósito

`IdentityLogin.vue` implementa el formulario de login reutilizable.

## Qué hace

Si el usuario ya está autenticado muestra `IdentityProfileView`; si no, renderiza el formulario y emite `loginSuccess` tras un login correcto.
