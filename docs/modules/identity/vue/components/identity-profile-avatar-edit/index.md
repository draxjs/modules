# IdentityProfileAvatarEdit

## Propósito

`IdentityProfileAvatarEdit.vue` añade edición al avatar del perfil.

## Qué hace

Muestra un ícono de edición al hover, dispara un input `file` oculto y delega la subida a `useAuth().changeAvatar()`.
