# PermissionSelector

## Propósito

`PermissionSelector.vue` ofrece una UI agrupada para seleccionar permisos.

## Qué hace

Carga permisos desde `useRole()`, los agrupa por entidad y permite seleccionar múltiples valores usando `v-item-group`.
