# IdentityProfileView

## Propósito

`IdentityProfileView.vue` presenta una vista resumida del usuario autenticado.

## Qué hace

Muestra avatar editable, username, email, tenant y rol leyendo `authUser` desde `AuthStore`.
