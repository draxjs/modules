# IdentityProfileAvatar

## Propósito

`IdentityProfileAvatar.vue` renderiza el avatar actual del usuario autenticado.

## Qué hace

Lee `authUser` desde `AuthStore`, muestra la imagen si existe y emite `click` cuando el avatar se presiona.
