# base.css

## Propósito

`base.css` define la base visual heredada del template Vue para `identity-vue`.

## Qué incluye

Variables de color, soporte para `prefers-color-scheme`, reset básico y estilos globales del `body`.
