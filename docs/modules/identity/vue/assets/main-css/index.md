# main.css

## Propósito

`main.css` compone estilos base de la app demo de `identity-vue`.

## Qué hace

Importa `base.css` y define layout centrado para `#app`, estilos de links y comportamiento responsive.
