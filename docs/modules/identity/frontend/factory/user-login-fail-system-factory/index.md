# UserLoginFailSystemFactory

## Propósito

`UserLoginFailSystemFactory` construye el `UserLoginFailSystem`.

## Qué hace

Encapsula la elección entre `UserLoginFailGqlProvider` y `UserLoginFailRestProvider`.
