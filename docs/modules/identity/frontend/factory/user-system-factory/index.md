# UserSystemFactory

## Propósito

`UserSystemFactory` crea el `UserSystem` principal del frontend.

## Qué hace

Instancia `UserGqlProvider` o `UserRestProvider` y mantiene una única instancia del system.
