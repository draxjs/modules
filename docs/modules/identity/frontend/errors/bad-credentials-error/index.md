# BadCredentialsError

## Propósito

`BadCredentialsError` representa credenciales inválidas del lado cliente.

## Detalles

Es una clase mínima que extiende `Error`, usa `BadCredentialsError` como `name` y por defecto expone el mensaje `Bad credentials`.
