# RoleSystem

## Propósito

`RoleSystem` encapsula la lógica de aplicación para roles.

## Qué hace

Delega en el provider y protege métodos opcionales como `search`, `create`, `update`, `delete` y `export`.
