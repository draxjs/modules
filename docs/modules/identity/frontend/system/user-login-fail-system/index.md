# UserLoginFailSystem

## Propósito

`UserLoginFailSystem` encapsula el acceso a intentos fallidos.

## Qué hace

Expone `paginate`, `groupBy` y `export`, validando si el provider soporta las operaciones opcionales.
