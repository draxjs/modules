# TenantSystem

## Propósito

`TenantSystem` encapsula la capa de aplicación para tenants.

## Qué hace

Expone `fetchTenant`, CRUD, `paginate` y `export`, validando que el provider implemente los métodos opcionales.
