# AuthRestProvider

## Propósito

`AuthRestProvider` implementa `IAuthProvider` sobre endpoints REST.

## Qué hace

Resuelve login, `me`, cambio de tenant, cambio de password propio, recovery, registro y avatar usando rutas `/api/auth/*` y `/api/users/*`.
