# RoleGqlProvider

## Propósito

`RoleGqlProvider` implementa el CRUD y consultas auxiliares de roles sobre GraphQL.

## Qué hace

Expone `search`, `fetchPermissions`, `fetchRole`, `create`, `update`, `delete` y `paginate` usando un set común de `gqlFields`.
