# UserApiKeyGqlProvider

## Propósito

`UserApiKeyGqlProvider` implementa la gestión de API keys sobre GraphQL.

## Qué hace

Expone `search`, `create`, `update`, `delete` y `paginate`, con campos como `secret`, `ipv4`, `ipv6` y `createdBy`.
