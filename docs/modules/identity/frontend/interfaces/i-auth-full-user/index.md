# IAuthFullUser

## Propósito

`IAuthFullUser` modela el usuario autenticado expandido en frontend.

## Qué incluye

Identificador, credenciales visibles, estado activo, avatar, `role` e `tenant` opcional.
