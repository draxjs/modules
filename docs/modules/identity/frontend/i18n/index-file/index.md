# index

## Propósito

El `index.ts` de `identity-front` combina todos los bloques i18n del módulo.

## Qué hace

Usa `deepmerge` para unificar mensajes de auth, usuario, rol, tenant, API key, sesiones, intentos fallidos, permisos, auditoría y validaciones.
