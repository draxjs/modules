# Components

## Propósito

La carpeta contiene los bloques visuales reutilizables del módulo Vue.

## Piezas principales

- `AuditDashboard`: filtro por fechas y render del dashboard.
- `AuditView`: vista detallada de una entrada de auditoría.
