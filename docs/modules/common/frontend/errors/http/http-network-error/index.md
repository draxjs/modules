# HttpNetworkError

## Propósito

`HttpNetworkError` representa fallos de red previos a una respuesta HTTP válida.

## Detalles

Extiende `HttpError`, usa `statusCode` `0` y el mensaje `Network error`.
