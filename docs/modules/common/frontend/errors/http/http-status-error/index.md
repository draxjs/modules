# HttpStatusError

## Propósito

`HttpStatusError` representa respuestas HTTP no exitosas.

## Detalles

Guarda el `statusCode` y el `body` del backend, y construye un mensaje como `Status code error: <statusCode>`.
