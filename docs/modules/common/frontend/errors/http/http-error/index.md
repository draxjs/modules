# HttpError

## Propósito

`HttpError` es la clase base de errores REST.

## Qué expone

Implementa `IRestError` con `statusCode`, `body` y `message`, y sirve como contenedor común para los casos derivados.
