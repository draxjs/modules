# NetworkError

## Propósito

`NetworkError` representa fallos de red del lado cliente.

## Detalles

Envuelve el error original, usa `error.network` como mensaje y ofrece `consoleError()` para logging.
