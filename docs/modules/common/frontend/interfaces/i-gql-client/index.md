# IGqlClient

## Propósito

`IGqlClient.ts` define el contrato del cliente GraphQL.

## Piezas principales

- `IGqlOptions`, con `headers` y `timeout`
- `IGqlClient`, con `query`, `mutation`, `upload`, `addHeader` y `removeHeader`
