# IGqlError

## Propósito

`IGqlError` tipa la forma de un error GraphQL consumible por `common-front`.

## Qué incluye

`message`, `path`, `extensions` opcionales con `code` e `inputErrors`, e `isBadUserInput`.
