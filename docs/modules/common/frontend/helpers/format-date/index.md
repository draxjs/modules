# formatDate

## Propósito

`formatDate` formatea una fecha ISO a una fecha local corta.

## Detalles

Usa `Intl.DateTimeFormat`, devuelve string vacío para valores inválidos y usa `es-AR` por defecto.
