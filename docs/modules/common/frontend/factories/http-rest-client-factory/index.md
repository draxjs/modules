# HttpRestClientFactory

## Propósito

`HttpRestClientFactory` crea y reutiliza una única instancia de `HttpRestClient`.

## Detalles

Permite configurar una URL base alternativa, pero preserva el patrón singleton.
