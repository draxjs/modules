# CommonConfig

## Propósito

`CommonConfig` define las claves de configuración base del backend común.

## Qué incluye

Variables para engine de base de datos, archivos SQLite, URI de MongoDB, URL base, directorio de archivos, tamaño máximo de upload y parámetros de caché como TTL y URL de Redis.
