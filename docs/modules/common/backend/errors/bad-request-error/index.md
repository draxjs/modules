# BadRequestError

## Propósito

`BadRequestError` representa errores de entrada inválida o parámetros mal formados.

## Detalles

Permite personalizar `message` e `i18nMessage` y responde con `statusCode` `400`.
