# MethodNotAllowedError

## Propósito

`MethodNotAllowedError` representa un método HTTP no permitido.

## Detalles

Responde con `405` y la clave i18n `error.methodNotAllowed`. El mensaje implementado hoy coincide con el de permisos insuficientes.
