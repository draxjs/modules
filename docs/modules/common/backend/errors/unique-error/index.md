# UniqueError

## Propósito

`UniqueError` expresa violaciones de unicidad por entidad y campo.

## Detalles

Guarda `entity` y `field`, responde con `400` y construye el mensaje a partir de ambos datos.
