# ForbiddenError

## Propósito

`ForbiddenError` modela accesos autenticados sin permisos suficientes.

## Detalles

Devuelve `403`, el mensaje fijo `Forbidden. Insufficient permissions.` y la clave i18n `error.forbidden`.
