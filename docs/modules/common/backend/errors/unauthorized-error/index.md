# UnauthorizedError

## Propósito

`UnauthorizedError` representa accesos sin autenticación válida.

## Detalles

Responde con `401`, devuelve siempre `Unauthorized` como mensaje público y usa `error.unauthorized` como clave i18n.
