# InvalidIdError

## Propósito

`InvalidIdError` indica que el identificador recibido no cumple el formato esperado.

## Detalles

Guarda el id original, responde con `400` y construye un mensaje como `Invalid ID: <id>`.
