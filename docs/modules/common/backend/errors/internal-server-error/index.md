# InternalServerError

## Propósito

`InternalServerError` es el fallback para excepciones no controladas en backend.

## Detalles

Responde con `500`, un mensaje genérico y la clave i18n `error.internal_server_error`.
