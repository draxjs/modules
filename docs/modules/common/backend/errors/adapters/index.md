# Adapters

## Propósito

La subcarpeta `adapters` convierte errores de librerías externas a `ValidationError` o a errores compatibles con GraphQL.

## Casos cubiertos

MongoDB, Mongoose, SQLite, Zod y el puente desde `ValidationError` hacia `GraphQLError`.
