# ValidationFieldError

## Propósito

`ValidationFieldError` representa un error de validación puntual.

## Detalles

Guarda `field`, `value` y `reason`. Se usa como bloque de construcción para `ValidationError`, no como respuesta HTTP completa.
