# LimitError

## Propósito

`LimitError` se usa cuando un valor supera un máximo permitido.

## Detalles

Guarda `maximum` y `value`, responde con `400` y expone un mensaje detallado para el caso de límite excedido.
