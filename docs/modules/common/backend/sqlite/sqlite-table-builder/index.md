# SqliteTableBuilder

## Propósito

`SqliteTableBuilder` crea y evoluciona tablas SQLite en base a una definición de columnas.

## Qué resuelve

- creación inicial de la tabla
- verificación de existencia de tabla y columnas
- agregado incremental de columnas nuevas
- soporte para columnas únicas y custom SQL
