# SqlQueryFilter

## Propósito

`SqlQueryFilter` convierte `IQueryFilter[]` en cláusulas `WHERE` parametrizadas.

## Qué devuelve

Un objeto con `where` y `params`, listo para integrarse con consultas SQL preparadas.
