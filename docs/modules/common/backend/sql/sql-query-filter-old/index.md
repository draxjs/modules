# SqlQueryFilterOld

## Propósito

`SqlQueryFilterOld` es la versión anterior del builder de filtros SQL.

## Diferencia principal

A diferencia de `SqlQueryFilter`, compone el `WHERE` como string interpolado sin arreglo de parámetros separado.
