# SqlSort

## Propósito

`SqlSort` construye la cláusula `ORDER BY` para consultas SQL simples.

## Qué hace

Si recibe `orderBy`, devuelve una cadena con `ASC` o `DESC`; si no, retorna string vacío.
