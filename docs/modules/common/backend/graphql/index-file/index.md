# index

## Propósito

El `index.ts` de `graphql` arma el punto de entrada del módulo GraphQL común.

## Qué hace

Calcula el directorio actual y delega en `GraphqlMerger` la carga de typeDefs y resolvers para devolver ambos conjuntos juntos.
