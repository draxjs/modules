# StoreManager

## Propósito

`StoreManager` centraliza el guardado y borrado de archivos.

## Qué hace

Valida nombre, stream, extensiones, mimetypes y tamaño máximo; luego persiste el archivo con nombre aleatorio usando `StreamFileStore`.
