# CreateDirIfNotExist

## Propósito

`CreateDirIfNotExist.ts` expone `createDirIfNotExist`.

## Qué hace

Verifica con `fs.existsSync` si una carpeta existe y la crea de forma recursiva si hace falta.
