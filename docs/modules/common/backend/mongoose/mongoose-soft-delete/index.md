# MongooseSoftDelete

## Propósito

`MongooseSoftDelete` es un plugin de schema para borrado lógico.

## Qué agrega

Campos `deleted` y `deletedAt`, métodos `softDelete` y `softRestore`, y una query helper `isDeleted`.
