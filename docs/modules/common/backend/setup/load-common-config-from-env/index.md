# LoadCommonConfigFromEnv

## Propósito

`LoadCommonConfigFromEnv` carga en `DraxConfig` las variables de entorno declaradas en `CommonConfig`.

## Qué cubre

Engine de base de datos, archivos SQLite, URI de Mongo, URL base, directorio de archivos y tamaño máximo de upload.
