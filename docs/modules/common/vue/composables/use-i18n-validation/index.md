# useI18nValidation

## Propósito

`useI18nValidation` ayuda a traducir arreglos de errores de validación.

## Qué hace

Su función `$ta` toma `string[] | undefined` y devuelve una cadena unificada, traduciendo cada clave si existe en `vue-i18n`.
