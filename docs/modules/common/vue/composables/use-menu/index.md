# useMenu

## Propósito

`useMenu` concentra la lógica de visibilidad y estado de items de menú.

## Qué hace

Usa `useRoute`, `useAuth` y `useI18n` para traducir textos, verificar permisos, detectar autenticación y resolver qué item está activo.
