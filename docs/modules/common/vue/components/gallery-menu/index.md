# GalleryMenu

## Propósito

`GalleryMenu.vue` muestra un menú en formato galería.

## Qué hace

Consume `IMenuItem[]`, usa `useMenu` para filtrar por permisos y renderiza `MenuCard` para items simples o hijos visibles.
