# SidebarMenu

## Propósito

`SidebarMenu.vue` renderiza un menú lateral basado en `v-list`.

## Qué hace

Usa `useMenu` para decidir visibilidad por autenticación o permisos, y soporta items simples y grupos con hijos.
