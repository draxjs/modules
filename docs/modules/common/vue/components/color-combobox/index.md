# ColorCombobox

## Propósito

`ColorCombobox.vue` es un `v-select` para elegir colores de la paleta de Vuetify.

## Qué hace

Expone `v-model`, permite configurar variant, density, clearable, readonly y reglas, y renderiza avatar e item visual para cada color.
