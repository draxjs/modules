# IMenu

## Propósito

`IMenu.ts` define la forma de los menús compartidos entre frontend y Vue.

## Piezas principales

- `ILink`, con `name`
- `IMenuItem`, con `icon`, `text`, `link`, `gallery`, `children`, `permission` y `auth`
