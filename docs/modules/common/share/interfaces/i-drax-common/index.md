# IDraxCommon

## Propósito

`IDraxCommon.ts` define contratos básicos compartidos.

## Piezas principales

- `IDraxCommon`, con la propiedad `common`
- `IDraxDateFormatUnit`, con las unidades `year`, `month`, `day`, `hour`, `minute` y `second`
