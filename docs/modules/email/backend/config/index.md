# Config

## Propósito

La carpeta agrupa las claves de configuración del módulo.

## Piezas principales

- `EmailLayoutConfig`: variables de entorno para header, footer y estilos.
- `EmailTransportConfig`: variables de entorno para SMTP o Gmail.
